# AlphaQuant 使用文档

> A股量化 AI 系统 · 完整使用手册  
> 版本：v3.0 · 更新日期：2026-06

---

## 目录

1. [项目简介](#1-项目简介)
2. [快速开始](#2-快速开始)
3. [项目结构](#3-项目结构)
4. [交互式菜单](#4-交互式菜单)
5. [全局配置参数](#5-全局配置参数)
6. [数据层](#6-数据层)
7. [特征构建](#7-特征构建)
8. [模型架构](#8-模型架构)
9. [模型训练](#9-模型训练)
10. [历史回测](#10-历史回测)
11. [单股买卖点图](#11-单股买卖点图)
12. [模拟盘](#12-模拟盘)
13. [网页看板](#13-网页看板)
14. [常见问题](#14-常见问题)
15. [命令行参数速查](#15-命令行参数速查)

---

## 1. 项目简介

AlphaQuant 是一套面向 A 股市场的端到端量化 AI 系统。它从公开行情数据源自动抓取历史日K线，构建 16 个技术特征，训练 GRU + Attention 神经网络来预测个股"未来 5 日内涨幅超 5%"的概率，并在历史回测和每日模拟盘中验证信号质量。

### 系统架构一览

| 模块 | 技术 | 说明 |
|------|------|------|
| **数据层** | HTTP 直接抓取 | 搜狐（主）→ 腾讯（备）→ 新浪（备），三级自动降级；全 A 股约 5000 只 |
| **特征层** | NumPy 向量化 | 16 个技术特征，逐窗口 Z-Score 归一化 |
| **模型层** | PyTorch GRU | 两层 GRU + Scaled-Dot-Product Attention + 3 层 FC |
| **回测引擎** | Pandas 回测 | T+1、手续费、涨跌停、止损，同时对比 5 只基准指数 |
| **模拟盘** | 定时调度 | 每日 15:30 自动运行，维护虚拟账户状态 |
| **看板** | Flask | 本地网页，5 个页面展示账户、持仓、信号、交易记录、模型表现 |

### 核心设计原则

- **无前视偏差**：归一化在推理时逐窗口独立计算，不依赖未来数据
- **跨股票通用**：特征全部为相对量（收益率、比率），同一模型覆盖全 A 股
- **随机采样**：每次训练从全 A 股随机抽取，避免选股偏差
- **相对排名交易**：默认按当日概率排名买入前 N 只，比固定阈值更稳健

---

## 2. 快速开始

### 2.1 系统要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10 / macOS 12 / Linux | 同左 |
| Python | 3.10+ | 3.11 |
| 内存 | 4 GB | 8 GB+ |
| 磁盘 | 2 GB 可用空间 | 5 GB+（缓存大量股票数据） |
| 网络 | 能访问搜狐/腾讯/新浪 | 同左 |

> **不需要 GPU**，也不需要 conda。纯 CPU 即可运行。

---

### 2.2 Windows 安装（五步首次运行）

**第一步：安装 Python**

前往 https://www.python.org/downloads/ 下载 Python 3.10 或更高版本，安装时勾选"Add Python to PATH"。

**第二步：下载项目代码**

```
git clone <仓库地址>
cd AlphaQuant
```

**第三步：一键安装依赖**

双击运行 `install.bat`，或在命令提示符中执行：

```bat
install.bat
```

脚本会自动创建 `venv` 虚拟环境并安装所有依赖，首次约需 5～10 分钟。

**第四步：启动程序**

双击 `start.bat`，或在命令提示符中执行：

```bat
start.bat
```

**第五步：训练模型并回测**

启动后会显示交互式菜单，依次操作：

```
按 2  → 快速训练（随机 20 只股票，近 3 年，约 5～15 分钟）
按 4  → 历史回测（查看净值曲线和绩效报告）
按 v  → 查看生成的图表报告
```

---

### 2.3 Mac / Linux 安装

```bash
# 安装依赖
bash install.sh

# 激活虚拟环境并启动
source venv/bin/activate
python main.py
```

---

## 3. 项目结构

```
AlphaQuant/
├── main.py                  # 主入口：交互菜单 + CLI 参数解析
├── config.py                # 全局配置（股票池、路径、超参、交易参数）
├── install.bat              # Windows 一键安装脚本
├── start.bat                # Windows 一键启动脚本
├── install.sh               # Mac/Linux 安装脚本
├── requirements.txt         # Python 依赖列表
│
├── data/
│   ├── loader.py            # 数据加载：搜狐/腾讯/新浪三源，本地CSV缓存
│   ├── index_fetcher.py     # 基准指数（沪深300等5只）行情拉取
│   ├── realtime.py          # 实时行情（模拟盘用）
│   └── cache/               # 股票历史数据缓存（自动创建）
│
├── features/
│   └── builder.py           # 16个技术特征计算 + 逐窗口Z-Score归一化
│
├── models/
│   ├── lstm_model.py        # GRU + Attention 模型定义（类名保留LSTMModel）
│   ├── trainer.py           # 训练循环：AdamW + CosineAnnealingLR + 早停
│   └── saved/               # 模型权重文件（自动创建）
│
├── backtest/
│   ├── engine.py            # 回测引擎：T+1、手续费、涨跌停、5基准对比
│   └── metrics.py           # 绩效指标计算（夏普、最大回撤等）
│
├── paper_trading/
│   ├── scheduler.py         # 定时调度器（每日15:30触发）
│   └── executor.py          # 模拟盘执行器（信号计算、下单、账户管理）
│
├── dashboard/
│   └── app.py               # Flask 本地看板（5个页面）
│
├── diagnose/
│   ├── analyzer.py          # 个股诊断逻辑
│   └── report.py            # 诊断报告输出
│
├── utils/
│   └── viz.py               # 中文字体配置（matplotlib）
│
├── logs/                    # 运行日志、账户状态（自动创建）
│   ├── alphaquant.log       # 详细运行日志
│   ├── account_state.json   # 模拟盘账户状态
│   ├── signals_cache.json   # 今日信号缓存
│   └── peak_assets.json     # 账户峰值（用于组合止损）
│
└── reports/                 # 生成的图表和数据文件（自动创建）
    ├── training_report.png  # 训练曲线图（Loss + AUC）
    ├── backtest_report.png  # 回测综合报告图
    ├── signal_<代码>.png    # 单股买卖点图
    ├── trade_log.csv        # 回测交易明细
    └── signals_YYYYMMDD.xlsx # 导出的今日信号表格
```

---

## 4. 交互式菜单

直接运行 `python main.py`（无参数）即进入交互式菜单，是最主要的使用方式。

```
╔════════════════════════════════════════════════╗
║                                                ║
║      AlphaQuant — A股量化 AI 系统              ║
║                                                ║
╠════════════════════════════════════════════════╣
║  【训练 & 回测】                               ║
║  1  训练模型（完整模式，全部股票）             ║
║  2  训练模型（快速模式，随机10只/近2年）       ║
║  3  继续训练（在已有模型基础上增量学习）       ║
║  4  历史回测                                   ║
╠════════════════════════════════════════════════╣
║  【实盘 & 看板】                               ║
║  5  启动模拟盘调度器                           ║
║  6  启动网页看板                               ║
║  7  查看今日信号排行                           ║
╠════════════════════════════════════════════════╣
║  【工具】                                      ║
║  8  个股诊断                                   ║
║  9  单股买卖点图（K线 + 模型信号）             ║
║  v  查看训练/回测报告图表                      ║
║  e  导出今日信号到 Excel                       ║
║  r  重置虚拟账户                               ║
║                                                ║
║  0  退出                                       ║
╚════════════════════════════════════════════════╝
```

### 各选项说明

| 按键 | 功能 | 说明 |
|------|------|------|
| `1` | 完整训练 | 从全 A 股随机抽取 100 只（`FULL_STOCK_COUNT`），拉取 2015 年至今的历史数据进行训练。首次运行耗时较长（数据下载 + 训练）。可选是否强制重新下载数据。 |
| `2` | 快速训练 | 从全 A 股随机抽取 20 只（`QUICK_STOCK_COUNT`），只拉取近 3 年数据。适合初次体验或快速验证模型改动，一般 5～15 分钟内完成。 |
| `3` | 继续训练 | 加载 `models/saved/lstm_best.pt` 的权重，在此基础上继续训练（增量学习）。适合已有模型、希望用新数据微调时使用。若模型文件不存在，自动从头开始。 |
| `4` | 历史回测 | 需先训练好模型。回测前提示输入起止日期，默认为 2015-01-01 至今。会自动拉取 5 只基准指数数据进行对比，输出综合报告图和交易明细 CSV。 |
| `5` | 模拟盘调度器 | 启动后台定时任务，每日 15:30 自动计算信号并更新虚拟账户。`Ctrl+C` 停止。 |
| `6` | 网页看板 | 在本地启动 Flask 服务（默认 http://127.0.0.1:5000），提供账户总览、持仓、信号、历史交易、模型表现共 5 个页面。`Ctrl+C` 停止。 |
| `7` | 今日信号排行 | 读取信号缓存，按概率从高到低列出所有股票的买入概率和建议（买入/持仓/卖出）。 |
| `8` | 个股诊断 | 输入一个或多个股票代码（逗号分隔），输出详细诊断报告。可输入持仓成本价以计算盈亏。 |
| `9` | 单股买卖点图 | 输入股票代码和回测年数，生成 K 线走势 + 模型买卖点标注图，保存到 `reports/signal_<代码>.png` 并自动打开。 |
| `v` | 查看报告图表 | 列出 `reports/` 目录下所有 PNG 文件，可选择单个或全部用系统默认程序打开。 |
| `e` | 导出信号到 Excel | 将今日信号导出为 `reports/signals_YYYYMMDD.xlsx`，含两个 Sheet：今日信号 + 交易明细。未安装 openpyxl 时退回为 CSV 格式。 |
| `r` | 重置虚拟账户 | 将模拟盘账户恢复至初始资金（默认 100 万），清空持仓、暂停状态和峰值记录。操作前需二次确认（输入 yes）。 |
| `0` | 退出 | 退出程序。 |

---

## 5. 全局配置参数

所有参数集中在 `config.py`，修改后重启程序即生效。

### 5.1 路径配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `DATA_CACHE_DIR` | `data/cache/` | 股票历史数据缓存目录 |
| `MODEL_SAVE_DIR` | `models/saved/` | 模型权重保存目录 |
| `LOGS_DIR` | `logs/` | 日志和账户状态目录 |
| `REPORTS_DIR` | `reports/` | 报告图表和导出文件目录 |

### 5.2 股票采样配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `FULL_STOCK_COUNT` | `100` | 完整训练模式（选项 1）从全 A 股随机抽取的数量 |
| `QUICK_STOCK_COUNT` | `20` | 快速训练模式（选项 2）从全 A 股随机抽取的数量 |
| `QUICK_HISTORY_YEARS` | `3` | 快速模式只拉取近 N 年的历史数据 |
| `STOCK_LIST_CACHE` | `stock_list.json` | 全 A 股列表缓存文件名（7 天内复用，存于 `DATA_CACHE_DIR`） |

> **注意**：`STOCK_POOL` 列表是内置的精选 20 只股票，仅在新浪接口无法拉取全 A 股列表时作为备用。日常训练使用的是随机抽取的全 A 股，不固定于这 20 只。

### 5.3 数据配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `START_DATE` | `"20150101"` | 全量历史数据起始日期 |
| `WINDOW_SIZE` | `30` | 时间序列窗口大小（天），即模型每次看 30 个交易日的数据 |
| `FEATURE_DIM` | `16` | 特征维度，与 `FEATURE_NAMES` 列表长度一致 |
| `LABEL_HORIZON` | `5` | 标签定义：未来 N 个交易日内 |
| `LABEL_THRESHOLD` | `0.05` | 标签定义：未来 N 日内最高价涨幅超过此值（5%）则标记为正样本 |

### 5.4 模型超参

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `LSTM_HIDDEN1` | `128` | 第一层 GRU 的隐藏层大小 |
| `LSTM_HIDDEN2` | `64` | 第二层 GRU 的隐藏层大小 |
| `FC_HIDDEN` | `32` | 全连接层的基础隐藏大小 |
| `DROPOUT` | `0.3` | Dropout 比率（防过拟合） |
| `LEARNING_RATE` | `5e-4` | 初始学习率 |
| `BATCH_SIZE` | `512` | 批大小，较大批次减少迭代次数、加快每轮速度 |
| `MAX_EPOCHS` | `300` | 最大训练轮次，实际由早停提前终止 |
| `EARLY_STOP_PATIENCE` | `30` | 早停耐心值：连续 30 轮验证 AUC 无提升则停止 |
| `LR_PATIENCE` | `7` | 学习率调度相关参数 |
| `TRAIN_RATIO` | `0.8` | 训练集比例（按时间顺序划分，前 80% 训练，后 20% 验证） |

### 5.5 回测与模拟盘交易参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `INIT_CAPITAL` | `1,000,000` | 初始资金（100 万） |
| `COMMISSION_BUY` | `0.0003` | 买入手续费（万三） |
| `COMMISSION_SELL` | `0.0013` | 卖出手续费（含印花税，万十三） |
| `RISK_FREE_RATE` | `0.03` | 无风险利率（用于夏普比率计算） |
| `BUY_THRESHOLD` | `0.65` | 绝对阈值模式：概率超过此值触发买入信号 |
| `SELL_THRESHOLD` | `0.35` | 绝对阈值模式：概率低于此值触发卖出信号 |
| `MAX_POSITION_RATIO` | `0.20` | 单股最大仓位占总资产的比例（20%） |
| `MAX_HOLDINGS` | `5` | 最大同时持仓数量 |
| `STOP_LOSS_RATIO` | `-0.07` | 单股止损线（亏损超过 7% 触发）|
| `PORTFOLIO_STOP` | `-0.12` | 组合止损线（账户总资产回撤超 12% 暂停交易） |
| `SUSPEND_DAYS` | `3` | 触发组合止损后暂停交易天数 |

### 5.6 相对排名交易模式

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `RELATIVE_RANK_MODE` | `True` | **默认开启**。True = 每天按概率排名，买前 TOP_N 只；False = 传统绝对阈值模式 |
| `TOP_N_BUY` | `3` | 相对模式下每日最多买入的候选数（实际受 `MAX_HOLDINGS` 限制） |
| `RANK_SELL_BOTTOM` | `0.40` | 相对模式下，当日排名处于后 40% 分位的持仓股票触发卖出 |

> **小白提示**：相对排名模式（`RELATIVE_RANK_MODE=True`）适合模型刚训练完、概率分布较为保守时，确保每天都有信号产生；绝对阈值模式适合模型训练充分、对预测概率有更高置信度时。

### 5.7 其他配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `CPU_THREAD_RATIO` | `0.5` | 训练时使用的 CPU 核心比例（0.5 = 一半核心；0 = 全部；正整数 = 固定数量） |
| `DAILY_RUN_TIME` | `"15:30"` | 模拟盘每日触发时间 |
| `DASHBOARD_HOST` | `"127.0.0.1"` | 看板监听地址 |
| `DASHBOARD_PORT` | `5000` | 看板监听端口 |

---

## 6. 数据层

### 6.1 三级数据源级联

系统不依赖任何第三方数据库 SDK，完全通过 HTTP 直接抓取公开行情数据。

```
_fetch_kline(code, start, end)
    ├── 1. 搜狐前复权日K线（主力源）
    │      q.stock.sohu.com/hisHq
    │      字段：开/收/高/低/量/成交额/换手率
    │      失败 → 
    ├── 2. 腾讯前复权日K线（第一备用）
    │      web.ifzq.gtimg.cn/appstock/app/fqkline/get
    │      字段：开/收/高/低/量（无成交额和换手率，填0）
    │      失败 → 
    └── 3. 新浪财经日K线（第二备用）
           money.finance.sina.com.cn/quotes_service/api/json_v2.php
           字段：开/收/高/低/量/成交额（非前复权）
           单次最多 1023 条，自动分批拉取
```

任一数据源成功返回数据即停止尝试，不会多余请求其他来源。当某源出现限流（HTTP 503/429）时，自动指数退避重试（最多 3～4 次）。

### 6.2 全 A 股列表

股票代码列表通过新浪财经接口动态拉取（沪 A + 深 A，含科创板、创业板），结果缓存到 `data/cache/stock_list.json`，**7 天内复用**，无需每次联网。若拉取失败，退回到 `config.py` 中内置的 20 只精选股票池。

```python
# 列表来源
http://vip.stock.finance.sina.com.cn/quotes_service/api/json_v2.php
/Market_Center.getHQNodeData?node=hs_a   # 沪A
/Market_Center.getHQNodeData?node=sz_a   # 深A
```

### 6.3 本地缓存策略

| 情况 | 行为 |
|------|------|
| 本地无缓存 | 全量拉取从 `START_DATE` 至今，保存为 `data/cache/<代码>.csv` |
| 缓存存在且当日已更新 | 直接读取缓存，不发起网络请求 |
| 缓存存在但数据不是最新 | 只拉取缺失部分（增量更新），追加到缓存文件 |
| `force_refresh=True` | 强制全量重新下载，覆盖缓存 |

### 6.4 并发限速

批量加载时使用 4 个并发线程（`ThreadPoolExecutor(max_workers=4)`），并通过信号量（`Semaphore(4)`）和随机延迟（0.3～1.0 秒/次）控制请求频率。每完成 5 只股票额外休息 2～3 秒，避免被数据源识别为爬虫而封禁。

User-Agent 从 6 个常见浏览器 UA 中随机轮换。

---

## 7. 特征构建

### 7.1 全部 16 个特征

| # | 特征名 | 类别 | 计算方式 |
|---|--------|------|---------|
| 1 | `ret_1d` | 价格动量 | 当日涨跌幅：`close/prev_close - 1` |
| 2 | `ret_5d` | 价格动量 | 近 5 日累积收益率 |
| 3 | `ret_20d` | 价格动量 | 近 20 日累积收益率 |
| 4 | `ma5_dev` | 均线偏离 | 收盘价偏离 5 日均线的百分比：`close/MA5 - 1` |
| 5 | `ma10_dev` | 均线偏离 | 收盘价偏离 10 日均线的百分比 |
| 6 | `ma20_dev` | 均线偏离 | 收盘价偏离 20 日均线的百分比 |
| 7 | `ma60_dev` | 均线偏离 | 收盘价偏离 60 日均线的百分比 |
| 8 | `vol_ratio` | 量能 | 量比：当日成交量 / 20 日均量，上限截断为 10 |
| 9 | `vol_trend` | 量能 | 量能趋势：5 日均量 / 20 日均量，上限截断为 5 |
| 10 | `turnover` | 量能 | 换手率（%），上限截断为 30 |
| 11 | `amplitude` | 价格形态 | 振幅：`(high - low) / prev_close`，上限 20% |
| 12 | `close_strength` | 价格形态 | 收盘强度：`(close - low) / (high - low)`，1 表示收于当日最高价 |
| 13 | `open_gap` | 价格形态 | 跳空幅度：`open / prev_close - 1`，截断为 ±10% |
| 14 | `rsi14` | 震荡指标 | RSI(14)，归一化到 [0, 1] |
| 15 | `macd_hist` | 震荡指标 | MACD 柱状图，用 20 日滚动标准差归一化 |
| 16 | `bb_pos` | 震荡指标 | 布林带位置：`(close - 下轨) / (上轨 - 下轨)`，0 = 下轨，1 = 上轨 |

所有特征均为相对量（百分比、比率），不含绝对价格，因此同一模型可以通用于所有 A 股。

### 7.2 标签定义

```
label = 1  如果  max(future_high[t+1 .. t+5]) / close[t] - 1 > 5%
label = 0  否则
```

即：当前时刻买入，未来 5 个交易日内的最高价涨幅超过 5% 记为正样本。

### 7.3 逐窗口 Z-Score 归一化

每个输入窗口（30 个交易日 × 16 个特征）独立归一化：

```python
mean = window.mean(axis=0)   # 对时间轴求每个特征的均值
std  = window.std(axis=0)    # 对时间轴求每个特征的标准差
X_norm = (window - mean) / std
```

**为什么不用全局 MinMaxScaler？**

全局 Scaler 需要在整个训练集上统计参数，推理时也依赖这些参数，实际部署中容易产生前视偏差（训练集的分布泄露到推理时）。逐窗口 Z-Score 完全基于当前窗口的统计量，天然防止前视偏差，也无需保存 Scaler 文件。

---

## 8. 模型架构

### 8.1 网络结构

```
输入  (batch, 30, 16)     # 30天窗口，16个特征
  │
  ▼ BatchNorm1d(16)       # 对特征维度归一化
  │
  ▼ GRU-1 (hidden=128)   # 捕捉短期时序依赖
  ▼ LayerNorm + Dropout
  │
  ▼ GRU-2 (hidden=64)    # 捕捉更高层的时序特征
  ▼ LayerNorm + Dropout
  │
  ▼ Scaled-Dot-Product Attention
  │   query = 最后一个时间步
  │   scores = query · keys^T / sqrt(64)
  │   context = softmax(scores) · values
  │   让模型自动找到 30 天中最关键的时间步
  │
  ▼ FC(64→64) + BN + ReLU + Dropout
  ▼ FC(64→32) + ReLU
  ▼ FC(32→1) + Sigmoid
  │
输出  (batch, 1)          # 买入概率 [0, 1]
```

### 8.2 为什么用 GRU 而不是 LSTM？

| 对比项 | LSTM | GRU |
|--------|------|-----|
| 门控数量 | 3（输入/遗忘/输出） | 2（更新/重置） |
| 参数量 | 相对较多 | 比 LSTM 少约 25% |
| CPU 速度 | — | 比 LSTM 快 25～30% |
| 序列建模能力 | 强 | 相当，短中期序列尤其接近 |

对于 30 日的日K线序列，GRU 的能力与 LSTM 相当，但在 CPU 上运行速度更快，适合无 GPU 的本地环境。

> **注意**：代码中类名保留为 `LSTMModel`，这是为了兼容已保存的模型权重，避免重新训练。

### 8.3 损失函数

使用带正样本权重的二元交叉熵（解决样本不平衡问题）：

```
pos_weight = 负样本数 / 正样本数
loss = -(pos_weight × y × log(pred) + (1-y) × log(1-pred))
```

A 股中正样本（5 日涨幅 > 5%）比例约 20～30%，pos_weight 通常为 2～4，确保模型不会因为正样本少而偏向预测全为负。

---

## 9. 模型训练

### 9.1 三种训练模式

| 模式 | 菜单 | CLI | 数据量 | 适用场景 |
|------|------|-----|--------|---------|
| 完整训练 | `1` | `--mode train` | 随机 100 只 + 全量历史 | 正式使用，首次建立模型 |
| 快速训练 | `2` | `--mode train --quick` | 随机 20 只 + 近 3 年 | 初次体验、验证改动 |
| 继续训练 | `3` | 暂无 CLI | 与选项 1 相同 | 已有模型，用新数据微调 |

### 9.2 训练流程

```
1. 拉取全 A 股列表（新浪接口，7天缓存）
2. 随机采样 N 只股票
3. 4 线程并发下载/更新历史数据
4. 计算 16 个技术特征，构建 (N, 30, 16) 的窗口序列
5. 逐窗口 Z-Score 归一化
6. 按时间顺序划分训练集(80%)和验证集(20%)
7. 训练循环：最多 300 轮，连续 30 轮验证 AUC 无提升则早停
8. 保存最优模型权重到 models/saved/lstm_best.pt
9. 同时保存带时间戳的版本备份（如 lstm_20260601_142030_auc0.5823.pt）
10. 生成训练报告图 reports/training_report.png
```

### 9.3 优化器与调度器

| 组件 | 配置 |
|------|------|
| 优化器 | AdamW（`lr=5e-4`，`weight_decay=1e-4`） |
| 学习率调度 | CosineAnnealingLR：从 5e-4 平滑降到 1e-6，共 300 轮 |
| 梯度裁剪 | `clip_grad_norm_(model.parameters(), 1.0)` |
| GPU 加速 | 有 GPU 时自动启用混合精度（AMP），无 GPU 时纯 CPU |
| torch.compile | PyTorch 2.x 自动尝试启用 `aot_eager` 后端编译（首轮约 30 秒编译，之后更快） |

### 9.4 理解训练输出

```
第 1轮 | 损失(越低越好): 0.6234 | 训练识别率: 0.5103 | 验证识别率: 0.5087 → 持平
第 2轮 | 损失(越低越好): 0.5891 | 训练识别率: 0.5342 | 验证识别率: 0.5215 ↑ 提升
...
第 87轮 | 损失(越低越好): 0.4127 | 训练识别率: 0.6234 | 验证识别率: 0.5783 ↑ 提升
...
⏹ 早停：30 轮无提升，最佳验证识别率: 0.5812
```

| 指标 | 含义 | 参考值 |
|------|------|--------|
| 损失 | 越低越好，通常从 0.6+ 降到 0.4+ | 0.4～0.5 说明模型在学习 |
| 训练识别率（Train AUC） | 训练集上的 ROC-AUC | 正常收敛 0.6～0.7+ |
| 验证识别率（Val AUC） | **关键指标**，验证集上的 ROC-AUC | 0.55～0.65 为良好；< 0.52 说明需要更多数据 |

> AUC 解释：0.5 = 完全随机猜测；0.6 = 有一定预测能力；0.7+ = 较强预测能力。A 股环境下 0.55～0.65 已属实用范围。

### 9.5 训练产出文件

| 文件 | 位置 | 说明 |
|------|------|------|
| `lstm_best.pt` | `models/saved/` | 最优模型权重（验证 AUC 最高的那轮） |
| `lstm_YYYYMMDD_HHMMSS_aucX.XXXX.pt` | `models/saved/` | 带时间戳的版本备份 |
| `scaler.joblib` | `models/saved/` | 占位文件（逐窗口归一化不需要全局 Scaler，保留用于接口兼容） |
| `training_report.png` | `reports/` | 训练曲线图（Loss 曲线 + AUC 曲线） |

---

## 10. 历史回测

### 10.1 回测规则

| 规则 | 详情 |
|------|------|
| T+1 制度 | 当日买入，次日起才能卖出（`today_bought` 保护集） |
| 涨跌停限制 | 涨停时无法买入；跌停时无法卖出（科创/创业板 ±20%，其余 ±10%） |
| 手续费 | 买入 0.03%（`COMMISSION_BUY`），卖出 0.13%（`COMMISSION_SELL`，含印花税） |
| 单股止损 | 持仓成本亏损超 7%（`STOP_LOSS_RATIO`）时自动卖出 |
| 最大仓位 | 单股不超过总资产的 20%，最多同时持有 5 只 |

### 10.2 交易信号模式

**相对排名模式（默认，`RELATIVE_RANK_MODE=True`）：**

- 买入：每日按概率排名，买入前 `TOP_N_BUY`（默认 3）只持仓外的股票
- 卖出：持仓中当日排名处于后 40%（`RANK_SELL_BOTTOM=0.40`）的股票卖出
- 优势：即使模型整体概率偏低，每天也会产生信号

**绝对阈值模式（`RELATIVE_RANK_MODE=False`）：**

- 买入：概率 > `BUY_THRESHOLD`（0.65）时买入
- 卖出：概率 < `SELL_THRESHOLD`（0.35）时卖出
- 优势：信号更精准；缺点：模型训练不充分时可能无任何信号

### 10.3 基准指数对比

回测自动拉取以下 5 只基准指数，与策略净值并排展示：

| 基准 | 代码 | 线型 |
|------|------|------|
| 上证指数 | sh000001 | 橙色虚线 |
| 深证成指 | sz399001 | 紫色点划线 |
| 创业板指 | sz399006 | 青色点线 |
| 沪深 300 | sh000300 | 黄色虚线 |
| 上证 50 | sh000016 | 棕色点划线 |

夏普比率和超额收益以沪深 300 为基准计算。

### 10.4 输出报告

**终端输出绩效指标：**

| 指标 | 含义 |
|------|------|
| 总收益率 | 整个回测期间总盈亏 |
| 年化收益率 | 折算成每年的平均收益率 |
| 夏普比率 | 超额收益 / 波动率（>1 为佳） |
| Sortino 比率 | 超额收益 / 下行波动率 |
| Calmar 比率 | 年化收益率 / 最大回撤 |
| 最大回撤 | 从历史最高点到最低点的最大跌幅 |
| 胜率 | 盈利交易笔数 / 总交易笔数 |
| 交易次数 | 完整买卖对的总笔数 |
| 超额收益 | 相对沪深 300 的超出部分 |
| 最终资产 | 回测结束时的账户总价值 |

**图表报告（`reports/backtest_report.png`）：**

- 净值曲线对比（AlphaQuant + 5 指数）
- 回撤曲线
- 月度收益柱状图（绿涨红跌）
- 交易盈亏分布直方图（含胜率）
- 绩效指标文字汇总

**交易明细（`reports/trade_log.csv`）：**

记录每笔买入/卖出的日期、代码、价格、股数、手续费、单笔盈亏%和卖出原因（信号/止损/排名）。

---

## 11. 单股买卖点图

菜单选项 `9` 可为任意股票生成买卖点标注图。

### 操作步骤

```
按 9 → 输入股票代码（如 sh600519）→ 输入回测年数（默认 2）
```

### 图表内容

图表分为上下两个子图：

**上图（收盘价走势）：**
- 灰色折线：收盘价走势
- 红色向上三角（▲）：买入信号（概率 ≥ `BUY_THRESHOLD` = 0.65）
- 蓝色向下三角（▼）：卖出信号（概率 ≤ `SELL_THRESHOLD` = 0.35）
- 标题显示买入信号次数和卖出信号次数

**下图（模型概率）：**
- 紫色折线：模型输出的逐日买入概率
- 红色虚线：买入阈值（0.65）
- 蓝色虚线：卖出阈值（0.35）
- 红色填充区：概率高于买入阈值的区域
- 蓝色填充区：概率低于卖出阈值的区域

图表保存到 `reports/signal_<代码>.png`，并自动尝试用系统默认图片查看器打开。

---

## 12. 模拟盘

### 12.1 启动方式

菜单选项 `5`，或命令行 `python main.py --mode paper`。

运行后持续循环，`Ctrl+C` 停止。

### 12.2 每日执行流程

```
每天 15:30（DAILY_RUN_TIME）触发：
  1. 拉取当日最新行情数据
  2. 对所有股票计算买入概率
  3. 执行止损检查（单股 -7%，组合 -12%）
  4. 卖出排名垫底的持仓（相对排名模式）或低概率持仓（绝对阈值模式）
  5. 买入当日概率排名前 N 只候选股
  6. 更新账户状态（logs/account_state.json）
  7. 记录交易明细（logs/trades_YYYYMMDD.csv）
  8. 更新信号缓存（logs/signals_cache.json）
```

### 12.3 风险控制规则

| 规则 | 触发条件 | 处理方式 |
|------|---------|---------|
| 单股止损 | 持仓股票亏损超 7% | 立即卖出该股 |
| 组合止损 | 账户总资产回撤超 12% | 暂停买入操作 `SUSPEND_DAYS`（3）天 |
| 涨停保护 | 当日涨停 | 跳过该股的买入 |
| 跌停保护 | 当日跌停 | 跳过该股的卖出 |
| 仓位上限 | 单股超过总资产 20% | 按上限计算可买股数 |

### 12.4 账户重置

菜单选项 `r` 可重置虚拟账户，将现金恢复为初始 100 万，清空持仓和所有历史峰值记录。

---

## 13. 网页看板

菜单选项 `6` 启动本地 Flask 服务，默认地址：**http://127.0.0.1:5000**

### 5 个页面

| 页面 | 路由 | 内容 |
|------|------|------|
| 账户总览 | `/` | 账户总资产、现金、持仓市值、最近净值曲线 |
| 持仓明细 | `/holdings` | 当前持仓列表：代码、名称、持仓股数、成本价、最新价、浮盈 |
| 信号排行 | `/signals` | 今日各股买入概率排名，标注建议操作 |
| 历史交易 | `/trades` | 所有历史交易记录，支持按代码和日期筛选 |
| 模型表现 | `/performance` | 每日净值折线图、月度收益、关键绩效指标 |

> 看板为只读展示，所有数据从 `logs/` 目录读取，不会触发任何交易操作。

---

## 14. 常见问题

### Q1：数据下载很慢或部分股票无数据，怎么办？

系统使用三个数据源自动降级，不依赖任何付费接口。如果下载缓慢：

- 搜狐、腾讯、新浪均可在境内正常访问
- 遇到限流会自动退避重试，属正常现象，无需手动干预
- 快速模式（选项 2）只抽取 20 只，大幅减少下载量
- 已下载的数据缓存在 `data/cache/`，下次启动会直接读取

### Q2：程序报错"模型文件不存在"

请先训练模型（选项 1 或 2），`models/saved/lstm_best.pt` 生成后才能运行回测（选项 4）和模拟盘（选项 5）。

### Q3：回测没有任何交易记录

原因：所有日期的预测概率都低于 `BUY_THRESHOLD`（0.65），导致没有买入信号。

解决方法：
1. 确认 `RELATIVE_RANK_MODE=True`（默认已开启），相对排名模式下每天必然有信号产生
2. 若仍无交易，尝试用更多数据重新训练（选项 1 完整模式）
3. 或适当降低 `config.py` 中的 `BUY_THRESHOLD`（如改为 0.55）

### Q4：训练速度很慢

- 完整模式（100 只股票 + 2015 年至今）首次运行含大量下载，共约 30～60 分钟
- 快速模式（选项 2，20 只 + 近 3 年）约 5～15 分钟，推荐初次体验使用
- `CPU_THREAD_RATIO=0.5` 会占用一半 CPU 核心，如需更快可改为 `1.0`（使用全部核心）
- 有 GPU 时 PyTorch 会自动使用，速度提升明显

### Q5：验证 AUC 一直在 0.5 附近，没有提升

可能原因：
- 数据量太少（快速模式 20 只股票可能不够）：改用完整训练模式（选项 1）
- 训练轮次不够：最大 300 轮，耐心等待
- 随机抽样抽到了比较难预测的股票：重新训练换一批样本

### Q6：全 A 股列表拉取失败

程序会自动退回到 `config.py` 中内置的 20 只精选股票池（`STOCK_POOL`）继续运行，不影响正常使用。7 天后程序会再次尝试拉取最新列表。

### Q7：图表中出现乱码（方框代替中文）

系统已内置中文字体配置（`utils/viz.py`），会自动查找 SimHei 等中文字体。如果仍出现乱码，在 Linux 上安装中文字体：

```bash
sudo apt-get install fonts-wqy-zenhei
# 然后重启程序
```

### Q8：导出 Excel 时提示"openpyxl 未安装"

```bash
# 激活虚拟环境后安装
pip install openpyxl
```

安装后重新运行选项 `e` 即可导出标准 `.xlsx` 格式。

---

## 15. 命令行参数速查

如果需要在脚本或自动化流程中使用，可以通过命令行参数调用，无需进入交互菜单。

```bash
python main.py --mode <模式> [选项]
```

### 支持的模式

| 模式 | 说明 | 额外参数 |
|------|------|---------|
| `train` | 训练模型（完整模式） | `--quick`（快速模式），`--refresh`（强制重新下载数据） |
| `backtest` | 历史回测 | — |
| `paper` | 启动模拟盘调度器 | — |
| `dashboard` | 启动网页看板 | — |
| `signal` | 打印今日信号排行 | — |
| `diagnose` | 个股诊断 | `--stock <代码>`，`--cost <成本价>` |
| `reset` | 重置虚拟账户 | — |

### 示例

```bash
# 完整模式训练
python main.py --mode train

# 快速模式训练
python main.py --mode train --quick

# 强制重新下载数据再训练
python main.py --mode train --refresh

# 历史回测（交互式输入起止日期）
python main.py --mode backtest

# 启动网页看板
python main.py --mode dashboard

# 诊断单只股票
python main.py --mode diagnose --stock sh600519

# 诊断并附带持仓成本价
python main.py --mode diagnose --stock sh600519 --cost 1650

# 查看今日信号
python main.py --mode signal

# 重置虚拟账户
python main.py --mode reset
```

> **注意**：`继续训练`（菜单选项 3）目前只能通过交互菜单使用，CLI 暂不支持 `--resume` 参数。

---

*文档版本 v3.0，与 AlphaQuant 当前代码库同步。如有疑问，详细运行日志请查看 `logs/alphaquant.log`。*
